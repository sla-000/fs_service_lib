import 'package:test/test.dart';

void main() {
  group(
    'IO functions tests - ',
    () {
      group(
        'readFromIn tests - ',
        () {
          // todo readFromIn tests
          test(
            'name tests',
            () {
              // expect(
              //   pathUtils.name(
              //     'projects/projectId/databases/databaseId/documents/ ',
              //   ),
              //   equals(''),
              // );
            },
          );
        },
      );

      group(
        'writeToOut tests - ',
        () {
          // todo writeToOut tests
          test(
            'name tests',
            () {
              // expect(
              //   pathUtils.parent('$path/qwer'),
              //   equals(path),
              // );
            },
          );
        },
      );
    },
  );
}
