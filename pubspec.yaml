name: fs_service_lib
description: Firestore service library. Allows to create, delete, update documents and collections 
  using service account.
version: 2.0.3
repository: https://github.com/sla-000/fs_service_lib

environment:
  sdk: ^3.7.0

dependencies:
  _discoveryapis_commons: ^1.0.6
  googleapis: ^16.0.0
  googleapis_auth: ^2.3.3
  hive_ce: ^2.19.3
  http: ^1.1.0
  meta: ^1.10.0

dev_dependencies:
  mocktail: ^1.0.1
  test: ^1.21.0

